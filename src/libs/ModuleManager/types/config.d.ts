/* eslint-disable @typescript-eslint/no-unused-vars */

type moduleConfig = {
  response_deletion_time: number;
  reloading: boolean;
  colours: {
    error: number;
    success: number;
    warn: number;
    standby: number;
    neutral: number;
  };
  disabled: string[];
}
