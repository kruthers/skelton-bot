/**
 * Convert a set of strings into a string (formatted)
 * @param set the input set
 */
export declare function StringSetToSting(set: Set<string>): string;
/**
 * Convert a array of strings into a string (formatted)
 * @param array the input array
 */
export declare function StringIteratorToSting(it: IterableIterator<string>): string;
